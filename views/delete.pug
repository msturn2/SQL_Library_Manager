extends layout

block content
  h2= book.title
  p 
    | Are you sure you want to delete 
    em= book.title
    | ?
  form(action="/books/" + book.id + "/delete", method="POST")
    a(class="button", href="/books/" + book.id) No! Take me back! 
    | 
    input(type="submit", value="Yes, I'm sure")